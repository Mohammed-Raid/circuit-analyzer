# Redirige vers composant.py — gardé pour la compatibilité des tests existants.
from circuit_analyzer.composant import (
    Composant as Component, lire_netlist as parse_file,
    charger_bibliotheque as load_library,
)
